# Docker role documentation

# docker role

Purpose:
- Install Docker Engine and CLI
- Enable Docker service
- Configure non-root Docker access

Guarantees:
- Docker is installed from official repo
- Docker daemon is running
- Users can run Docker without sudo
